name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node: ['20', '22']
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: npm

      - run: npm ci

      - name: Build the extension
        run: npm run build

      # The product's core claim, checked mechanically rather than by review.
      # Fails on any absolute URL to a non-HubSpot host in the built bundle, on
      # any reference to server-client, and on host_permissions widening beyond
      # hubspot.com.
      - name: No network calls in the extension
        run: npm run check:no-network

      # Allowlist check: every large integer in a committed file must be a known
      # synthetic value. A fresh capture dropped in without scrubbing fails here
      # rather than reaching the public repo.
      - name: No real portal data in tracked files
        run: npm run check:no-portal-data

      # The AI context block describes what the export did to the file. This
      # fails when the popup grows an export option that the block's
      # MODIFICATIONS table has never heard of, because the block would then
      # describe the file as it was one feature ago and look complete doing it.
      - name: The AI context block still describes every export option
        run: npm run check:ai-context

      - run: npm test
