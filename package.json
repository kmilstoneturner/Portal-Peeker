{
  "name": "portal-peeker",
  "private": true,
  "version": "1.0.0",
  "description": "Chrome extension for HubSpot admins: capture, inspect, and diff the JSON behind HubSpot workflows.",
  "license": "SEE LICENSE IN LICENSE",
  "type": "module",
  "workspaces": [
    "packages/*",
    "extension"
  ],
  "scripts": {
    "build": "node tools/build.mjs",
    "check:ai-context": "node tools/check-ai-context.mjs",
    "check:no-network": "node tools/check-no-network.mjs",
    "check:no-portal-data": "node tools/check-no-portal-data.mjs",
    "pretest": "node tools/build.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify": "npm run build && npm run check:no-network && npm run check:no-portal-data && npm run check:ai-context && npm run test"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
